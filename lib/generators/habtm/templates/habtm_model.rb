# frozen_string_literal: true
class <%= model_name %> < ApplicationRecord
  # == Relationships ========================================================
  # belongs_to :store
  # belongs_to :status
end
