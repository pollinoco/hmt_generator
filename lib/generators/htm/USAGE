Description:
    Generates has and belongs to many relationship for two given models

Example:
    rails generate htm user job

    This will create:
       * Migration for generating jobs_users table, with index
       * Pasting htm into each model
    reversible! (rails destroy htm user job)
